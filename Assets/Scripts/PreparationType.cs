using UnityEngine;

[CreateAssetMenu(fileName = "NewPrepType", menuName = "YesChef/PreparationType")]
public class PreparationType : ScriptableObject
{
    // Intentionally left empty. The object reference itself is the ID.
}
